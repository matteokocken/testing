server {
    server_name app.matteokocken.fr;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    root   /usr/share/nginx/html;
    index index.html;

    charset utf-8;


    location / {
        proxy_pass http://51.83.78.158:4000;
    }

    location = /robots.txt  { access_log off; log_not_found off; }

    access_log off;

    location ~ /\.(?!well-known).* {
        deny all;
    }
}


server {
    server_name api.matteokocken.fr;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    root   /usr/share/nginx/html;
    index index.html;

    charset utf-8;


    location / {
        proxy_pass http://51.83.78.158:5000;
    }

    location = /robots.txt  { access_log off; log_not_found off; }

    access_log off;

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
